{
  "name": "javascript",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "generate:env": "rm -rf dist && mkdir dist && env-cmd -f ./.env node ./scripts/env.js",
    "build": "rm -rf dist && mkdir dist && node ./scripts/env.js && cp -r node_modules/@portabl/js-sync-with-portabl/dist/index.umd.js ./dist/index.umd.js",
    "dev": "yarn generate:env && http-server -p 3002",
    "lint": "eslint ."
  },
  "dependencies": {
    "@portabl/js-sync-with-portabl": "^0.0.3"
  },
  "devDependencies": {
    "env-cmd": "^10.1.0",
    "eslint": "^8.23.1 ",
    "eslint-config-custom": "*",
    "http-server": "^14.1.1"
  },
  "workspaces": {
    "nohoist": [
      "@portabl/js-sync-with-portabl"
    ]
  }
}
